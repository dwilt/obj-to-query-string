{
  "name": "obj-to-query-string",
  "version": "1.0.2",
  "description": "A standalone js function to convert an object to a query string",
  "main": "lib/index.js",
  "scripts": {
    "transform": "babel src --out-dir lib",
    "minify": "uglifyjs index.js",
    "test": "npm run transform && tape -r babel-register tests/**/*.js",
    "build": "npm run test && npm run transform && npm run minify",
    "publish": "npm run build"
  },
  "author": {
    "name": "Dan Wilt",
    "email": "dwilt4rville@gmail.com",
    "url": "https://github.com/dwilt"
  },
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.11.4",
    "babel-preset-es2015": "^6.13.2",
    "babel-register": "^6.11.6",
    "tape": "^4.6.0",
    "uglify-js": "^2.7.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/dwilt/obj-to-query-string"
  }
}
